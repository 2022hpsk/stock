# CLAUDE.md — AI 协作指引

## 开始任何工作前必读
1. [docs/01-开发规范.md](docs/01-开发规范.md) — **强制规范，红线 R1–R7 不可违反**
2. [docs/02-系统架构.md](docs/02-系统架构.md) — 分层与依赖方向
3. [docs/03-功能规格.md](docs/03-功能规格.md) — 功能编号与验收标准
4. [docs/08-差距分析与设计补强.md](docs/08-差距分析与设计补强.md) — 已识别的陷阱与"明确不做"边界
5. 涉及情报模块时另读 [docs/07-信息情报模块.md](docs/07-信息情报模块.md)（红线 I-R1–I-R6）
6. 涉及界面时另读 [docs/09-可视化界面规格.md](docs/09-可视化界面规格.md)
7. 涉及大模型时另读 [docs/10-大模型集成规格.md](docs/10-大模型集成规格.md)（红线 LR1–LR8）
8. 涉及账户/持仓时另读 [docs/11-持仓账本规格.md](docs/11-持仓账本规格.md)

## 不可违反的红线速查
| | |
|---|---|
| R1 | 金额用 `Decimal`，禁止 `float` |
| R2 | 禁止未来函数，财务数据必须 PIT（按 `ann_date`） |
| R3 | 时间必须 tz-aware（Asia/Shanghai），禁止 `datetime.now()`，走 `infra.clock` |
| R4 | 复权口径必须显式：研究用 `hfq`，下单/展示用 `none` |
| R5 | 下单必须经过 `RiskEngine` + 人工确认；默认 `PaperBroker`，真实通道需 `--live` |
| R6 | 每条建议必须可追溯可复现（数据指纹 + 策略版本 + 参数哈希） |
| R7 | 严禁提交密钥、资金账号、真实持仓 |
| R8 | 交易流水只追加不修改；持仓/批次/快照全部由流水重放得出，可随时重建 |
| LR1 | LLM 输出不得直接成为下单方向、数量、价格；`risk`/`portfolio`/`execution` 禁止 import `llm` |
| LR2 | LLM 对打分的影响有界（α 默认 0.15、硬上限 0.20）且可一键关闭，关闭后系统须完整可用 |
| LR3 | LLM 调用全量缓存快照；**回测强制 `replay`**，实时调用抛 `LLMLiveCallInBacktestError` |
| LR4 | 回测中 LLM 只看 `as_of` 之前材料，且禁止用于价格/收益预测（防训练集泄漏） |
| I-R1 | 情报不得单独触发买入；只能解释、有界软调节、或单向风险否决 |
| I-R4 | 进入解释的情报必须带原文链接与发布时间 |

## 易踩的坑（08 号文档已列全，这里是最高频的几个）
- **幸存者偏差**：退市标的必须永久保留在数据湖；universe 查询必须带 `as_of`。
- **label 样本清洗**：入场日涨停不可买的样本必须剔除，否则 IC 严重虚高。
- **分红与红利税**：回测必须入账现金分红；卖出按持股期限扣税（≤1月 20%/1月-1年 10%/>1年 免）。
- **比例风控挡不住计算基数出错**：必须有独立的绝对金额硬闸（A10），且阈值手工设定。
- **UI 不能成为风控后门**：A 类规则界面上无关闭入口；`web`/`cli` 只能依赖 `services`。
- **过拟合**：每次回测尝试都要记录；DSR<0 或 PBO>0.5 禁止进实盘候选池。
- **LLM 训练集泄漏**：绝不问模型"这只股票会涨吗"。LLM 只做文本理解与归纳，预测交给量化因子。
- **LLM 回测**：必须先 `llm backfill` 预计算缓存，回测走 `replay`；实时调用会抛异常。
- **持仓必须做批次级 FIFO**：只存平均成本算不出红利税分档、持有期收益、免税倒计时。

## 工作流
```bash
make install     # 首次
make check       # 每次提交前必跑（lint + mypy + 分层检查 + 测试）
```
- 分支策略：**仅 `main` 一个分支**，trunk-based。完成一组有意义的变更即提交推送。
- 提交信息用 Conventional Commits；`strategy` / `risk` 类改动的提交正文必须附回测对比结论。
- 改架构或规格时，**同一提交内**更新对应文档。

## 常见陷阱
- 新增依赖前先确认是否已有等价能力；重量级依赖需在提交正文说明必要性。
- 写测试时时间必须注入（`FrozenClock`），禁止依赖 `date.today()`。
- 数据源适配器测试用录制 fixture 回放，禁止 CI 打真实网络（标记 `@pytest.mark.network`）。
- 任何涉及真实资金的默认值取最保守一侧。
- 修 bug 先写复现失败的回归测试。
